<%= form_for @application , remote: true do |f|%>
	<table class="cv_tables form_table">
		<tr><td>
			<%= f.hidden_field :vacancy_id, value: session[:current_vacancy] %>
			<h25>Cover Letter</h25>
			</td>			
		</tr>
		<tr>
			<td>
				<%= f.cktext_area :cover_letter %></td>
		</tr>
		
		<tr><td colspan="2"></td></tr>
	</table>
 <div class='field' >  
   <%= f.label :references %> <br/>
    <% for reference in  current_profile.references %>     
        <%= check_box_tag "application[reference_ids][]", reference.id, @application.references.include?(reference) %>
        <%= raw reference.referee_name %>       
    <% end %> 
  </div>
	<%=f.submit 'Apply', class: "btn btn-primary right"%>
<% end %>

